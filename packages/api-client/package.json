{
  "name": "@splashsphere/api-client",
  "version": "0.0.1",
  "private": true,
  "description": "Shared HTTP client for SplashSphere admin + POS apps. Wraps fetch with JSON handling, bearer-token injection, and ProblemDetails error throwing.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
