/**
 * Re-export of `@splashsphere/api-client`. Existing `@/lib/api-client`
 * imports keep working unchanged. New code can import from the shared
 * package directly.
 */
export { apiClient } from '@splashsphere/api-client'
export type { ApiClient } from '@splashsphere/api-client'
